const path = require('path')
const exec = require('child_process').exec

const tests = path.normalize('./node_modules/.bin/_mocha') + ' ' + path.normalize('test') + ' --colors --bail'

exec(tests,function(error, stdout, stderr){
  console.log(stdout)
})
